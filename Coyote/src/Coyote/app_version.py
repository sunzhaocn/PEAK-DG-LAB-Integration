"""Coyote application version.

Only change APP_VERSION when publishing a new application build.
The GitHub Release tag should use the same number with an optional leading v.
"""

APP_VERSION = "0.0.4"
