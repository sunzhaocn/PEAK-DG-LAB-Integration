// Compile-time reference stubs for GitHub Actions only.
//
// This project intentionally contains declarations/signatures only. It does not
// contain PEAK game implementation code. Coyote is loaded by the real PEAK
// process at runtime, where references bind to the game's Assembly-CSharp.dll.

using UnityEngine;

public class Character : MonoBehaviour
{
}
